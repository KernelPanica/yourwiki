[pytest]
DJANGO_SETTINGS_MODULE = config.test_settings
python_files = test_*.py
testpaths = tests
addopts = --strict-markers
markers =
    browser: Browser checks using Playwright (opt in with RUN_BROWSER=1)
    live_storage: Remote storage checks using private test credentials (opt in)
